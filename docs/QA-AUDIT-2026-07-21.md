# OmniRad — تقرير تدقيق الجودة الآلي (21 يوليو 2026)

فحص آلي لكل صفحات `pages/` (22) ووحدات `modules/` (37) على GitHub@main.
مصنّف بالأولوية. **لم يُنفَّذ أي إصلاح — بانتظار قرار الأولويات.**

---

## 🔴 P1 — عالية (✅ مكتملة 21 يوليو 2026)

**1. ملف مكرّر ميّت: `modules/series-slices.js` (7KB)** — ✅ **حُذف من المستودع.**
نسخة قديمة غير مرجوعة من أي صفحة (النسخة المعتمدة `modules/data/series-slices.js` v3، 37KB). خطر التباس/استخدام خاطئ مستقبلاً. → **حذف.**

**2. تنويه «تعليمي فقط»** — ✅ أُضيف footer موحّد ثنائي اللغة إلى 9 صفحات (admin · anatomy-review · clinic · contributors · daily · mnemonics · my-progress · review · srs).

**3. أخطاء 404 عامّة** — ✅ (أ) أُنشئ `index-assets/patterns/arabic-geometry.svg`؛ (ب) أُنشئ `index-assets/icons/favicon.svg` + حقن `<link rel=icon>` من مصدر واحد في nav.js.

**4. `omnirad-term.js` — `A.resolveToId`** — ✅ مُصلَح ومنشور (`?v=resolveid`).

---

## 🟠 P2 — متوسطة (✅ 5-6 مكتملة · 7 اختياري)

**5. `alert()` → `toast()`** — ✅ أُضيف `OmniRadToast` مشترك في nav.js؛ استُبدلت كل `alert()` في admin.js · studio-app.js · studio.html · daily.html.

**6. رسائل غير مترجمة** — ✅ كل رسائل toast الجديدة ثنائية اللغة (عربي/إنجليزي).

**7. بصمات الكاش غير موحّدة**
وحدات البيانات المشتركة `?v=loinc-r2`؛ العارض `?v=bc-cine`؛ term `?v=resolveid`؛ السلاسل `?v=series-v3`. يعمل، لكن يُفضّل مخطّط إصدار موحّد (مثل `?v=YYYYMMDD`).

---

## 🟡 P3 — منخفضة (تحسينات)

**8. محتوى تعليمي ثابت (مقصود — مرحلة مؤجَّلة)**
daily.html (5 أسئلة مضمّنة) · mnemonics · srs — بيانات ثابتة بانتظار ربط DB (طبقة Learn الموثّقة). ليس خطأ؛ للتتبّع.

**9. وصولية (a11y) — يتطلّب فحصاً يدوياً**
تباين ألوان الوضع النهاري (dim) · تنقّل لوحة المفاتيح على الأدوات المخصّصة · ARIA على المودالات · بدائل نصية. لم يُفحَص آلياً.

---

## ✅ نظيف (لا ملاحظات)
- **لا تسريب أسرار** — لا `service_role` ولا مفاتيح في الفرونت (كل مطابقات البحث كانت CSS mask-image).
- **لا TODO/FIXME/HACK** في كود الصفحات.
- الأخطاء الوظيفية المبلّغة سابقاً (bulk-upload الفارغة · قسم Series الثابت · related-series · زر إيقاف العارض) **مُصلَحة**.

---

## ترتيب المعالجة المقترح
1. P1-3 (404 العامّة) — أوضح أثر بصري في Console.
2. P1-1 (حذف الملف الميّت) — سريع.
3. P1-2 (التنويه الناقص) — امتثال إلزامي.
4. P2-5/6 (توحيد toast + ترجمة) — احترافية.
5. P3 (a11y) — جلسة فحص يدوي مخصّصة.
