# Copy this file to .env (same folder) and fill in real values locally.
# .env is git-ignored — NEVER commit real secrets. service_role stays local only.
SUPABASE_URL=https://YOUR-PROJECT.supabase.co
SUPABASE_SERVICE_KEY=          # paste service_role key in .env only, not here
SUPABASE_BUCKET=atlas
